package zombie.Lua;

import se.krka.kahlua.vm.KahluaTable;

// Hand-written stub, NOT decompiled game code — only the single field this project's
// production code actually reads (see LuaGlobalTierProvider.java). See KahluaTable.java in
// this same stubs/ tree for the full rationale.
public class LuaManager {
    public static KahluaTable env;
}
